import React from "react";
import PropTypes from "prop-types";
import { CheckBox } from "@material-ui/icons";

const Checktermini = ({text}) => {
    return(  
      <div></div>
    ); 
}
 
Subtitle.propTypes = {
    text: PropTypes.string.isRequired,
};

export default Checktermini;